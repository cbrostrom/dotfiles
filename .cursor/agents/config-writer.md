---
name: config-writer
description: Decides where to store new AI/agent behavior and creates or updates it. Knows the thin-wrapper architecture (AGENTS.md, shared skills, Cursor wrappers, rules). Checks for overlap before creating. Triggered by `.add`, "add behavior", "where does this go", "create a rule for", "make a skill that", "new agent for".
---

> Load shared skill: `~/.agents/skills/config-writer/SKILL.md` — covers placement decision framework, overlap checking, our architecture layers, and output format.

## Scope

Always outline placement + plan and wait for approval before creating or editing any files.
