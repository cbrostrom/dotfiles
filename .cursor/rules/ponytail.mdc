---
description: Deprecated — see core.mdc simple-first heuristic.
alwaysApply: false
---

# Build ladder (deprecated)

Replaced by **Simple-first** in `core.mdc`: simpler way exists → do it. Otherwise minimum code.

For rigor mode only: YAGNI → reuse in codebase → stdlib → minimum. Never skip: trust boundaries, data-loss, security, a11y.
