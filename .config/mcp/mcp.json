{
  "mcpServers": {
    "mcporter": {
      "command": "npx",
      "args": ["mcporter", "serve", "--stdio"],
      "env": {
        "MCPORTER_CONFIG": "${HOME}/.mcporter/mcporter.json"
      }
    }
  }
}
