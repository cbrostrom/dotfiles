# dotfiles module enable/disable overrides
# Format: one module name per line
#   !module  — disable (files kept, module skipped on bootstrap)
#    module  — explicit enable (override MODULE_DEFAULT_ENABLED=false)
# Lines starting with # are comments. Blank lines ignored.
# Full list: ./bootstrap.sh --list

# Editor: Zed disabled — using VSCodium
!zed

# Terminal multiplexer: switched away from Zellij
!zellij

# Secret manager: Bitwarden CLI for env-injected tokens
rbw

# Terminal multiplexer: agent-aware session persistence + SSH remote attach
herdr
herdr-autostart
