---
description: Run a shell command in the project directory. Usage `/sh git status`
allowed-tools: Bash
---

Run this command in the project workspace:

```bash
$ARGUMENTS
```

Reply with stdout and stderr only. Last line: `exit: <code>`. No commentary.
