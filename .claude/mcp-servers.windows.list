# Claude Code MCP servers — Windows (PowerShell) overrides.
# Loaded in addition to mcp-servers.list on Windows hosts.
# Bootstrap for Windows: TODO (PowerShell script not yet implemented).
# Until then, register manually:
#   claude mcp add engram -- engram mcp

# Override base entry: native Windows binary (on PATH after go install)
engram | engram | mcp
