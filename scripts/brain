#!/usr/bin/env bash
# brain/kb — deprecated; forwards to higgins
# Stage 1 shim: prints one notice to stderr, then execs the Go binary.
# Remove after Stage 4 (when kb is fully retired).
echo "kb: deprecated — use 'higgins' directly (this shim will be removed in Stage 4)" >&2
exec higgins "$@"
